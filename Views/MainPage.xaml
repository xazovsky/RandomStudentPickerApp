<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="RandomStudentPickerApp.Views.MainPage"
    Title="System losowania osoby">

    <StackLayout Padding="20" Spacing="15">

        <Label Text="Wybierz klasę:" FontSize="Medium"/>
        <Picker x:Name="classPicker" SelectedIndexChanged="OnClassSelected" />

        <Button Text="Losuj ucznia" Clicked="OnPickStudentClicked"/>
        <Button Text="Dodaj ucznia" Clicked="OnAddStudentClicked"/>
        <Button Text="Zapisz klasę" Clicked="OnSaveClassClicked"/>
        <Button Text="Utwórz nową klasę" Clicked="OnCreateNewClassClicked" />


        <Label x:Name="resultLabel"
               FontSize="Large"
               TextColor="DarkRed"
               HorizontalOptions="Center" />

        <CollectionView x:Name="studentsList">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Horizontal">
                        <Label Text="{Binding Name}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </StackLayout>
</ContentPage>
